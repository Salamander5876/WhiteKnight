# 🛡️ WhiteKnight VPN - VLESS + XHTTP Auto Installer

Автоматический установщик VPN сервера на базе **VLESS** протокола с **XHTTP** транспортом для обхода региональных блокировок и DPI (Deep Packet Inspection).

## 🎯 Особенности

✅ **Максимальное шифрование** - TLS 1.3 с AES-256-GCM и ChaCha20-Poly1305
✅ **Обход блокировок** - XHTTP транспорт для маскировки под обычный HTTPS трафик
✅ **Полная автоматизация** - установка за 2-3 минуты
✅ **Без домена** - работает на чистом IP адресе
✅ **QR-код** - для быстрого подключения
✅ **Поддержка AmneziaVPN** - готовый VLESS ключ

## 📋 Требования

- **VPS сервер** с Ubuntu 20.04+ или Debian 10+
- **Root доступ** (sudo)
- **Открытые порты**: 22 (SSH), 443 (VLESS)
- **Минимум 512MB RAM**
- **Чистая установка** (рекомендуется)

## 🚀 Быстрая установка

### Шаг 1: Подключение к VPS

Подключитесь к вашему серверу через SSH:

```bash
ssh root@ВАШ_IP_АДРЕС
```

### Шаг 2: Скачивание скрипта

```bash
wget https://raw.githubusercontent.com/Salamander5876/WhiteKnight/main/install-vless.sh
```

**ИЛИ** если у вас уже есть файл, загрузите его на сервер:

```bash
scp install-vless.sh root@ВАШ_IP_АДРЕС:/root/
```

### Шаг 3: Установка прав и запуск

```bash
chmod +x install-vless.sh
sudo ./install-vless.sh
```

### Шаг 4: Дождитесь завершения

Скрипт автоматически:
- Установит все зависимости
- Установит и настроит Xray-core
- Создаст самоподписанный сертификат
- Настроит файрвол
- Сгенерирует UUID и конфигурацию
- Выведет VLESS ключ и QR-код

## 📱 Настройка AmneziaVPN

### На компьютере (Windows/macOS/Linux):

1. Скачайте **AmneziaVPN** с официального сайта: https://amnezia.org
2. Установите и запустите приложение
3. Нажмите **"Настроить свой сервер"** → **"У меня есть данные для подключения"**
4. Выберите протокол **VLESS**
5. Вставьте **VLESS ключ**, который выдал скрипт
6. Нажмите **"Подключиться"**

### На телефоне (Android/iOS):

1. Установите **AmneziaVPN** из Google Play или App Store
2. Отсканируйте **QR-код**, который вывел скрипт
3. Или вручную добавьте сервер, вставив VLESS ключ
4. Нажмите **"Подключиться"**

## 📊 Формат VLESS ключа

После установки вы получите ключ в формате:

```
vless://UUID@IP:443?encryption=none&security=tls&type=xhttp&host=IP&path=/ПУТЬ&sni=IP&alpn=h2,http/1.1#WhiteKnight-VPN
```

**Скопируйте его полностью** и вставьте в AmneziaVPN!

## 🔧 Управление сервером

### Проверка статуса:
```bash
systemctl status xray
```

### Остановка сервиса:
```bash
systemctl stop xray
```

### Запуск сервиса:
```bash
systemctl start xray
```

### Перезапуск сервиса:
```bash
systemctl restart xray
```

### Просмотр логов в реальном времени:
```bash
journalctl -u xray -f
```

### Просмотр конфигурации:
```bash
cat /usr/local/etc/xray/config.json
```

### Просмотр VLESS ключа:
```bash
cat /root/vless_config.txt
```

### Полная информация:
```bash
cat /root/whiteknight_info.txt
```

## 🔐 Безопасность

Скрипт настраивает максимальный уровень безопасности:

- **TLS 1.3** - последняя версия протокола шифрования
- **AES-256-GCM** - 256-битное шифрование
- **ChaCha20-Poly1305** - альтернативный стойкий шифр
- **XHTTP транспорт** - маскировка под обычный HTTPS
- **Рандомный путь** - уникальный путь для каждой установки
- **Sniffing** - оптимизация маршрутизации трафика
- **UFW файрвол** - защита от нежелательных подключений

## 🛠️ Ручная настройка (опционально)

### Изменение порта:

Откройте конфигурацию:
```bash
nano /usr/local/etc/xray/config.json
```

Найдите строку `"port": 443` и измените на нужный порт.

Не забудьте открыть порт в файрволе:
```bash
ufw allow НОВЫЙ_ПОРТ/tcp
systemctl restart xray
```

### Добавление нескольких пользователей:

В секции `"clients"` добавьте новый объект:
```json
{
  "id": "НОВЫЙ_UUID",
  "level": 0,
  "email": "user2@whiteknight"
}
```

Сгенерировать UUID:
```bash
cat /proc/sys/kernel/random/uuid
```

## 📞 Поддержка и помощь

### Часто возникающие проблемы:

**1. Не могу подключиться к VPN:**
- Проверьте, что порт 443 открыт на вашем провайдере VPS
- Проверьте статус: `systemctl status xray`
- Проверьте логи: `journalctl -u xray -n 50`

**2. Скрипт выдает ошибку при установке:**
- Убедитесь, что запускаете от root: `sudo ./install-vless.sh`
- Проверьте, что у вас Ubuntu/Debian
- Проверьте подключение к интернету

**3. VPN подключается, но не работает интернет:**
- Проверьте DNS настройки в AmneziaVPN
- Попробуйте переподключиться
- Проверьте логи: `journalctl -u xray -f`

**4. Нужно переустановить:**
```bash
systemctl stop xray
rm -rf /usr/local/bin/xray /usr/local/etc/xray /var/log/xray
./install-vless.sh
```

## 🔄 Обновление Xray

Для обновления до последней версии:

```bash
bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install
systemctl restart xray
```

## 📁 Файлы и директории

- **Конфигурация**: `/usr/local/etc/xray/config.json`
- **Сертификаты**: `/usr/local/etc/xray/cert/`
- **Логи**: `/var/log/xray/`
- **VLESS ключ**: `/root/vless_config.txt`
- **Полная информация**: `/root/whiteknight_info.txt`
- **Исполняемый файл**: `/usr/local/bin/xray`

## 🌐 Альтернативные клиенты

Кроме AmneziaVPN, VLESS ключ работает в:

- **v2rayN** (Windows)
- **v2rayNG** (Android)
- **Shadowrocket** (iOS, платный)
- **Qv2ray** (Linux/Windows/macOS)
- **Hiddify** (Android/iOS)
- **NekoBox** (Android)

## ⚠️ Важные примечания

1. **Сохраните VLESS ключ** в безопасном месте
2. **Не делитесь ключом** с посторонними
3. **Регулярно проверяйте логи** на подозрительную активность
4. **Делайте бэкапы** конфигурации
5. **Используйте сложные пароли** для SSH доступа
6. **Рекомендуется настроить SSH ключи** вместо паролей

## 📈 Оптимизация производительности

### Для высоконагруженных серверов:

Отредактируйте `/etc/sysctl.conf`:
```bash
nano /etc/sysctl.conf
```

Добавьте:
```
net.core.default_qdisc=fq
net.ipv4.tcp_congestion_control=bbr
net.ipv4.tcp_fastopen=3
net.core.rmem_max=16777216
net.core.wmem_max=16777216
```

Примените изменения:
```bash
sysctl -p
```

## 🆘 Удаление

Если нужно полностью удалить VPN:

```bash
systemctl stop xray
systemctl disable xray
rm -rf /usr/local/bin/xray
rm -rf /usr/local/etc/xray
rm -rf /var/log/xray
rm /root/vless_config.txt
rm /root/whiteknight_info.txt
ufw delete allow 443/tcp
```

## 📜 Лицензия

MIT License - используйте свободно для личных и коммерческих целей.

## ⭐ Благодарности

- **Xray-core** - https://github.com/XTLS/Xray-core
- **AmneziaVPN** - https://amnezia.org
- Сообществу разработчиков anti-censorship инструментов

---

## 🎯 Сводка команд для копирования

### Установка:
```bash
wget https://raw.githubusercontent.com/Salamander5876/WhiteKnight/main/install-vless.sh
chmod +x install-vless.sh
sudo ./install-vless.sh
```

### Просмотр ключа:
```bash
cat /root/vless_config.txt
```

### Проверка статуса:
```bash
systemctl status xray
```

### Логи:
```bash
journalctl -u xray -f
```

---

**Создано с ❤️ для свободного интернета**

**Версия:** 1.0
**Дата обновления:** 2025-01-22
